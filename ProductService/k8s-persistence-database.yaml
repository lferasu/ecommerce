apiVersion: v1
kind: Service
metadata:
  name: productservice-db
spec:
  ports:
    - protocol: TCP
      port: 8085
      targetPort: auth-db-port
  selector:
    app: productservice-db
    tier: db
  clusterIP: None
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: productservice-db
spec:
  selector:
    matchLabels:
      app: productservice-db
      tier: db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: productservice-db
        tier: db
    spec:
      containers:
        - image: mysql:5.6
          name: productservice-db
          env:
            # Use secret in real usage
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: productservice-secrets
                  key: mysql-root-password
          ports:
            - containerPort: 3306
              name: auth-db-port
          volumeMounts:
            - name: productservice-db-persistent-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: productservice-db-persistent-storage
          persistentVolumeClaim:
            claimName: productservice-pv-claim
apiVersion: v1
kind: Service
metadata:
  name: cartservice-db
spec:
  ports:
    - protocol: TCP
      port: 8085
      targetPort: auth-db-port
  selector:
    app: cartservice-db
    tier: db
  clusterIP: None
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cartservice-db
spec:
  selector:
    matchLabels:
      app: cartservice-db
      tier: db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: cartservice-db
        tier: db
    spec:
      containers:
        - image: mysql:5.6
          name: cartservice-db
          env:
            # Use secret in real usage
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: cartservice-secrets
                  key: mysql-root-password
          ports:
            - containerPort: 3306
              name: auth-db-port
          volumeMounts:
            - name: cartservice-db-persistent-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: cartservice-db-persistent-storage
          persistentVolumeClaim:
            claimName: cartservice-pv-claim
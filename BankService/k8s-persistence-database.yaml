apiVersion: v1
kind: Service
metadata:
  name: bankservice-db
spec:
  ports:
    - protocol: TCP
      port: 8085
      targetPort: auth-db-port
  selector:
    app: bankservice-db
    tier: db
  clusterIP: None
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bankservice-db
spec:
  selector:
    matchLabels:
      app: bankservice-db
      tier: db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: bankservice-db
        tier: db
    spec:
      containers:
        - image: mysql:5.6
          name: bankservice-db
          env:
            # Use secret in real usage
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: bankservice-secrets
                  key: mysql-root-password
          ports:
            - containerPort: 3306
              name: auth-db-port
          volumeMounts:
            - name: bankservice-db-persistent-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: bankservice-db-persistent-storage
          persistentVolumeClaim:
            claimName: bankservice-pv-claim
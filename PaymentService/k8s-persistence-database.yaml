apiVersion: v1
kind: Service
metadata:
  name: paymentservice-db
spec:
  ports:
    - protocol: TCP
      port: 8085
      targetPort: auth-db-port
  selector:
    app: paymentservice-db
    tier: db
  clusterIP: None
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: paymentservice-db
spec:
  selector:
    matchLabels:
      app: paymentservice-db
      tier: db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: paymentservice-db
        tier: db
    spec:
      containers:
        - image: mysql:5.6
          name: paymentservice-db
          env:
            # Use secret in real usage
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: paymentservice-secrets
                  key: mysql-root-password
          ports:
            - containerPort: 3306
              name: auth-db-port
          volumeMounts:
            - name: paymentservice-db-persistent-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: paymentservice-db-persistent-storage
          persistentVolumeClaim:
            claimName: paymentservice-pv-claim